================================================================================
                        FUTURE ENGINEERING ROADMAP
================================================================================

[IMMEDIATE ENGINEERING]
1. Frontend History Hydration
   - CURRENT STATUS: Backend has `/api/history` endpoint, but Frontend ignores it.
   - TASK: Update App.js `useEffect` to fetch history on load.
   - CODE: 
     useEffect(() => {
       fetch(`${backendUrl}/api/history/${sessionId}`)
         .then(res => res.json())
         .then(data => setConversationHistory(data));
     }, []);

2. "Double-Talk" Handling
   - CURRENT STATUS: Sometimes VAD triggers on AI's own voice (Echo).
   - TASK: Fine-tune Web Audio API `echoCancellation: true` parameters.
   - EXP: Implement a "Debounce" timer in Rust to prevent micro-triggers.

3. Crisis Guardrails
   - CURRENT STATUS: Basic keyword detection ("kill myself").
   - TASK: Integrate a standalone "Guardrails" library (like Nvidia NeMo Guardrails)
     for enterprise-grade safety before the LLM even sees the prompt.

[PRODUCT POLISH]
4. Visual Feedback for "Thinking"
   - TASK: Add a distinct UI state (e.g., Pulsing Brain) when the "Parallel 
     Processing" (Emotion + Intent) is happening, so the user sees intelligence.

5. Dynamic Temperature
   - IDEA: Adjust LLM `temperature` based on Intent.
   - Venting: Temp 0.8 (More creative/warm).
   - Coaching: Temp 0.2 (More precise/factual).

[INFRASTRUCTURE]
6. Docker Composition
   - TASK: Create a `docker-compose.yml` to launch Backend + Frontend + Mongo 
     with a single command `docker-compose up`.

7. Latency Profiling
   - TASK: Add `time.time()` logs around the `asyncio.gather` block to measure
     exact millisecond savings from parallelization.
